/*export const createProfile = (bio) => {
    firebase
      .firestore()
      .collection('profile')
      .add({
        name: firebase.auth().currentUser.displayName,
        bio: bio,
      })
  }
  
  export const hashProfile = () => {
      window.location.hash = '#profile';
  };*/